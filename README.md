# AEGIS

Personal backup of the AEGIS Condor agent (XRPL dual-book maker + Gate dump-hedge / pump-release).

Not a hummingbot/condor fork. Drop `agents/aegis/` into a Condor checkout.

```
./.venv/bin/python agents/aegis/tests/test_aegis_pure.py
./.venv/bin/python agents/aegis/tests/validate_agent.py
```

Deck: `aegis-deck.html` · Script: `aegis-script.md`

Insurance only on this private repo. Do not push to hummingbot/condor from the live box.
